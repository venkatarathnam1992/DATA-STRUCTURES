#include<stdio.h>
#include<string.h>
int main()
{
int len=0,i;
char name[50];
printf("enter the name:");
scanf("%s",name);
//for(i=0;name[i]!='\0';i++)
//len++;
for(len=0;name[len]!='\0';len++);
for(i=len-1;i>=0;i--){
printf("%c",name[i]);

}
return 0;
}
